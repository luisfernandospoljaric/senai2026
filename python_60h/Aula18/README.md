
# 📘 Atividades

- Estruturas de decisão (`if`, `elif`, `else`)
- Estruturas de repetição (`for`, `while`)
- Tratamento de erros (`try`, `except`)
- Biblioteca **Pandas**

---

### Exercício 1 – Média de Notas
Peça ao usuário 3 notas. Utilize `try/except` para evitar erro de digitação. Calcule a média e informe se o aluno foi **aprovado (>= 7)** ou **reprovado**.

### Exercício 2 – Contador de Pares
Solicite ao usuário um número inteiro positivo e utilize um `for` para exibir todos os números **pares** de 0 até esse número.

### Exercício 3 – Cadastro Simples com Loop
Utilize um `while` para permitir o cadastro de nomes até que o usuário digite **"sair"**. Ao final, exiba todos os nomes cadastrados.

### Exercício 4 – Soma de Lista
Crie uma lista com números digitados pelo usuário. Use `try/except` para tratar erros e calcule a soma total usando um `for`.

### Exercício 5 – Pandas Básico
Crie um `DataFrame` com as colunas **Nome** e **Idade**. Exiba apenas as pessoas com idade maior ou igual a 18 anos.

---

### Exercício 6 – Validação de Login
Peça usuário e senha. O sistema deve permitir **3 tentativas**. Use `while`, `if` e `try/except`. Caso erre 3 vezes, bloqueie o acesso.

### Exercício 7 – Tabuada com Tratamento de Erro
Solicite um número e exiba a tabuada de 1 a 10. Use `try/except` para garantir que o valor seja numérico.

### Exercício 8 – Controle de Estoque com Pandas
Crie um `DataFrame` com **Produto**, **Preço** e **Estoque**. Mostre apenas os produtos com estoque menor que 5.

### Exercício 9 – Cálculo de Média com Pandas
Crie um `DataFrame` com alunos e notas. Calcule a média das notas e informe quais alunos ficaram abaixo da média.

### Exercício 10 – Sistema de Vendas
Utilize `while` para registrar vendas (valor). Use `try/except`. Ao final, exiba o **total vendido** e a **média das vendas** usando **Pandas**.

---

